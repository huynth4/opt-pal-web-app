[project]
name = "final-project-huynth4"
version = "0.1.0"
description = "This file is used to keep track of my project's dependencies"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=25.3.0",
    "pandas>=3.0.2",
]
